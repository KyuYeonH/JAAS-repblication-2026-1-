Caution: Please confirm the file names before loading the data. The analysis was conducted using Stata 16, while all visualizations and figures were generated using Python scripts.

The datasets used in the regression analysis are koreadata.dta and chinadata2.dta. After loading the data files, you can run the regression code directly. The date construction and variable generation steps shown in the preprocessing section of the code can be ignored, as the datasets provided here are already processed for the regression analysis.

If you would like access to the raw data, please feel free to contact me by email. Upon request, I will provide the raw data along with the code used for the preprocessing procedures, rather than the processed datasets used in the analysis.
